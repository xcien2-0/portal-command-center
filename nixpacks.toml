[phases.setup]
nixPkgs = ["python311", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = [
  "npm run build",
  "cp -r dist backend/dist_frontend"
]

[start]
cmd = "python backend/servidor_academia.py"
